Lesson 6
const nigerianStates =["Lagos","Abuja","Kaduna","Kwara","Imo","Anambara","Kogi"]
for( let i= 0; i < nigerianStates.length; i++){
let pickThree=""
let count=0
//console.log(nigerianStates[i].toLowerCase())
// if(nigerianStates[i].startsWith("K")){
// console.log(nigerianStates[i])
// }
// if(nigerianStates[i][3] === "K"){
// console.log(nigerianStates[i])
// }

for(let inner=0; inner<nigerianStates[i].length; inner++){

//console.log(nigerianStates[i][inner])
if(count < 3){
pickThree +=nigerianStates[i][inner]
count+=1
} else break

}
console.log(pickThree)
====================================

You can replace your whole outer loop + inner loop with that one line.

Here’s what it would look like in your existing file:

javascript
Copy
Edit
const nigerianStates = ["Lagos","Abuja","Kaduna","Kwara","Imo","Anambara","Kogi"]

nigerianStates.forEach(state => console.log(state.slice(0, 3)));
That’s it — no need for for loops, pickThree, or count, because:

.forEach() automatically loops through each element in the array.

.slice(0, 3) grabs the first 3 characters from the string.



===========================================

const studentProfile = {
name: "John Doe",
age: 20,
courses: [
    {title: "Mathematics", grades: [85, 90, 88]},{title: "Computer Science",grades: [92, 87, 94]}
],
hobbies: ["reading", "coding", "basketball"]
};

for (pickHobbies of studentProfile.hobbies) {
    console.log(pickHobbies)
}

//function that calcultates the average grade of mathematics and computer science SEPERATELY from the object given.
function averageGrade() {
    let averages = {}; // object to store average per course

    for (let i = 0; i < studentProfile.courses.length; i++) {
        let course = studentProfile.courses[i];
        let sum = 0;

        for (let j = 0; j < course.grades.length; j++) {
            sum += course.grades[j];
        }

        let average = sum / course.grades.length;
        averages[course.title] = average; // store result by course title //The [] means dynamic property name — whatever string is inside becomes the key in the object.
//The = assigns that key a value.
    }

    return averages;
}

console.log(averageGrade());
